json.extract! certificate, :id, :expired, :filename, :reference, :governance, :stock_item_id, :hdfs_filename, :expired_at, :created_at, :updated_at
json.url certificate_url(certificate, format: :json)
